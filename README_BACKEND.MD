# Backend – NestJS + MongoDB

## Stack
- NestJS
- MongoDB Atlas (Mongoose)
- JWT (access + refresh tokens)
- bcrypt for password hashing

## Setup
1. fill in `.env`
2. `npm install`
3. `npm run start:dev`
